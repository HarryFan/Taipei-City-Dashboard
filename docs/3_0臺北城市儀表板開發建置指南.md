
1. 大家好，歡迎收看本次線上課程臺北城市儀錶板建置指南本課程由臺北市政府資訊局規劃製作我是今天的講師臺北市政府資訊局的資深工程師蔡豪宣也是臺北城市儀錶板的開發與維護者這堂課將針對臺北城市儀錶板3.0的更新項目進行完整的介紹包含架構更新與新組件的設定流程協助大家快速掌握開發重點
2. 那我們就正式進入主題吧本課程主要有兩個重點第一部分會說明本次更新是如何調整臺北城市儀錶板的架構以支援多城市儀錶板建置並以目前的開源專案雙北共榮為例介紹導覽列的更新跟新版組件的架構第二個部分會介紹新增的圖表樣式文字單位圖目的是用於更清晰的呈現指標跟數值
3. 以上為本課程的更新重點概要與注意事項介紹下一個章節將會為為各位介紹資源多城市的儀錶板架構調整與應用謝謝大家謝謝大家!



1. 這次的資源多層次儀表版架構接下來這個課程的部分要跟各位說明就是我們這個新增的資源多層式儀表版的架構那先跟各位介紹就是為什麼我們這次要去做架構上的更新還有整體調整的細節那本次架構的更新的這個起點是來自於臺北市政府資訊局以及新北市政府資訊中心共同舉辦的2025年雙北城市設計節城市儀表版大黑客松希望能透過這個活動來促進雙北城市的數據串聯提供跨城市的即時資訊
2. 然後並推廣開圓原始碼與資料應用考量未來可能有更多的城市加入我們將臺北城市儀表版的架構升級為可支援多城市儀表版並同步釋出開圓專案雙北共榮儀表版雖然目前僅包含臺北跟雙北的內容但架構上已經有辦法支援多城市的能力未來可望可以整合更多城市的資料實現真正的跨域共榮
3. 歡迎各位開發者在未來的部分可以一起加入協作接下來跟各位介紹這一次架構面的調整在架構層面在過去儀表版2.0的組件是集中在Component這張表中設定一個Component裡面就會設定不同組件的內容但是為了要因應多城市的架構
4. 現在我們將Component跟組件的內容資訊的部分拆開為兩張表拆分成Component跟Query Chart的兩張表以便讓同一個組件可以依照城市的不同去切換不同的內容然後這個目的是為了能夠支援多城市的版本管理可以彈性的擴展組件的設定代表的是相同組件可以有多城市的顯示方式
5. 那我們來看一下技術文件在技術文件裡面其實我們已經有更新了簡單來說組件的部分是由四張表來儲存儀表版組件的配置分別為Component組件的內容會存在在Query Chart當中圖表跟地圖的配置的部分會儲存在Component Chart跟Component Maps當中那Component和Query Chart的表
6. 是透過Index的部分去做串連Component Chart則是透過Component跟Component Chart也是透過Index去做串連至於Component Maps因應不同的程式要顯示對應的圖層主要會透過Query Chart當中的Map Config ID跟Component Maps的ID欄去做串連簡單來說一個組件對應不同程式的顯示的內容是透過Query Chart去做設定的而顯示圖表跟地圖則是透過Component Chart
7. 跟Component Maps而顯示圖表跟地圖則是透過Component Chart跟Component Maps而顯示圖表跟地圖則是透過Component Chart和Component Maps決定這樣的架構可以讓組件可以更靈活的在不同程式版本間切換各程式的樣貌跟資料
8. 我們再回到簡報接下來這個部分要介紹的是為應應多程式版本的需求臺北城市儀表版更新了導覽列的設計在公開儀表版中左側的導覽列會依照程式分類顯示例如現在可以看到臺北儀表版跟雙北儀表版這兩個選項那點擊後可以展開或隱藏下面的儀表版組件我們來看一下畫面這個就是我們新版多數的設計
9. 我們可以展開設計畫面的設計我們可以展開設計畫面的設計畫面我們可以展開設計畫面的設計畫面我們可以展開設計畫面的設計畫面我們可以展開設計畫面的設計畫面我們可以展開設計畫面的設計畫面我們可以展開設計畫面的設計畫面我們可以展開設計畫面我們可以展開設計畫面的設計畫面我們可以展開設計畫面
10. 我們可以展開設計畫面我們可以展開設計畫面這個就是我們新版多層式架構的儀表版臺北層式儀表版3.0的新版的結果的架構的畫面那如各位所看到就是上面有臺北儀表版然後下面有雙北儀表版那點擊導覽列的部分可以去做導覽列的縮方
11. 那點擊縮小那這個部分會隱藏公共儀表版會隱藏到臺北跟雙北的內容那這是本次更新包含架構的更新跟新版導覽列設計的介紹那下一個章節會為新版組件的內容更新做說明謝謝大家謝謝大家
12. 謝謝大家



1. 這個部分主要要跟各位介紹新版主件架構的內容。那我們進到第一個章節。第一個部分要跟各位介紹所謂新版雙北共融儀表版主件的畫面。在畫面上大家可以看到主件可以，就是主件的展示大概是長這個樣子。就是左邊會有整體的圖表內容跟主件的名稱。那右邊部分的就是針對這個不同程式的主件內容，它會呈現對應的資訊。依照你在Query Chart的設定，會有對應的程式的內容。在雙北共融的這個主件裡面，可以看到在左上角的部分有一個選單。那透過下拉選單，點擊切換程式的部分，就可以顯示對應程式的資訊。
2. 那右邊的部分，就是在你選取對應程式的內容，就可以展現比如說在雙北的部分，它的主件的說明。因為你不同程式之間，它負責的單位可能不一樣。那負責的它想填寫的這個主件的說明的部分，跟實地上在雙北的情境，它的範例的使用的情境可能會不同。所以在新版的架構之下，我們透過Query Chart的設定，讓我們可以在不同程式間，去針對這個程式。它在這個主件，它要展示的情境，可以依照設定的部分。主件配置的部分，去讓這個主件可以同時有多程式樣態的呈現。那這個部分我們來看一下畫面。首先，首先再給各位介紹一下，就是我們新版的架構的操作的介面。那在上面的部分，依舊是臺北儀錶板。那在下面的部分是雙北的儀錶板。
3. 那在這次的範例組件裡面，會有長照關懷，跟務實交通的雙北的Dashboard。那在提供我們提供的範例組件裡面，長照關懷內容有包含這些，比如說像是，扶養筆跟老化指數的一個統計組件。然後高齡就業人口年增的這個結構的組件。全市年齡分區的這個組件。然後跟長照指標這個是一個新版的圖表。新的文字單位的圖表。這個在後面的章節會細細的為各位做介紹。那在跟之前的臺北儀錶板的不同的地方，是在於說本次雙北儀錶板的更新。在左上角的部分就是，陳如剛剛又講的就是它有一個下拉式的選單。
4. 在選單裡面就有包含前面不同程式的內容，可以去做切換。現在D4呈現的部分是雙北的這個圖表的內容。那在切換到臺北市之後，這部分就會呈現臺北市設定的內容。對，那在折線圖的部分也是依照就是你前面選取的內容去做呈現。那我們回到雙北的部分。在組件資訊裡面，這邊D4呈現的就是雙北。那左上角這邊就，在上面呢，在這個組件內容就會有這個標籤，跟你設定的單位，跟它的更新時間等等。那這個部分就是會呈現雙北的說明。那在範例清經的部分就會呈現雙北的翻譯行經。那相關的應用集，資料集的部分就有臺北，Data Taipei，然後跟新北市的公開資料平臺，Data， NTPC。
5. 那協作者目前也就是臺北市資訊局跟臺北市政府資訊局跟新北市政府資訊總心。那這個部分就是主件，新版主件的內容配置的設定的介紹。收.



1. 主件設定的部分。然後這個章節主要是要介紹說，在多程式的主件裡面，依照不同程式的內容，我們可以在 Query Chart 裡面去做設定。那目前的開源部分主要就，我們開源專案裡面主要提供TiBe跟雙Be這兩個版本。目前有開源的版本只有提供TiBe跟雙Be，TiBe是跟雙Be的內容。那它可以設定的部分，在主件的部分，我們可以設定名稱。那針對不同程式，它可能會有不同的來源，不同的來源，就是負責的局處的資料，跟它的資料類型。譬如說它的即時資料或是它的景態資料。那它的更新的頻率。那依照你設定，比如說你是設定雙北的主件的話，在標籤呈現上，它就會有雙北的標籤，跟臺北市的標籤。那在右邊部分主要是呈現資訊的部分。
2. 它有包含像可以分別設定組件的說明。那比如說可以設定範例的情境。它有它的Source，就是它的來自於的資料籍。它是它的相關資料是來自哪裡。可以分開設定。那協作者的部分，就有原本的TYC，就是臺北市資訊局。然後跟新北市政府的資訊中心。我們來看一下它在DB裡面的結構。那首先在我們先找到就是在Data Manager裡面。就是這個儀錶板它的系統的資料庫。
3. 我們先看Component這張表。我們先看Component這張表。這是我們這一次更新架構所提出來的這個範例的組件。其實它有包含就是有提供這些內容的組件。然後剛剛在畫面上看到，我們看到有一個組件叫俯仰比以及老化指數的部分。那我們找到這個index是dependency agent。那我們在query chart裡面也是同樣的去下這個查詢。那我們就會發現到它其實有兩筆資料。那它其實都有在對應的組件內容，它都有對應的設定。
4. 比如說我設定可以設定它的它的資料值的7次時間。比如說它在static 目前是就是屬於固定資料。那它的 source 目前都是就是各自單位負責的主基礎。然後它的描述跟各對應的描述的內容可以分開的去做設定。那以及它的資料來源，引用的資料籍等等。然後它的貢獻則有哪些。它的 query type，就是這個是在設定這個組件的圖表的格式。就是你的資料的緯度。然後後面就有對應的資料料用的語乳法。
5. 其實最主要的重點是要提及到這個 query chart 裡面其實有一個欄位叫做 city。那就是之前在前面段落其實都有提到這次的設定都目前開源專案裡面只有提供臺北跟雙北。雙北在 city 的設定叫做 metro or Taipei。然後這個欄位在 query chart 的設定目前是固定的。就是必填的欄位。所以建置的部分就要依照Default 的規則來建置。那我們再來切匯簡報。
6. OK 這個部分就是針對主件可以設定的範圍來做的介紹。所以內容的部分就是存在在 query chart 裡面。那依照 city 的不同來設置。謝謝大家



1. 接下來介紹圖表設定的部分。在圖表設定的部分，因為目前在圖表設計的在圖表設定上，不論是雙北的組件，或者是臺北市的組件，在設定上目前都是吃同一個設定。就是它在component chart裡面，目前都是共同通用的。新版組件設計就是可以針對不同程式來設定不同內容，但是圖表的樣式仍然會維持一致。也就是說在同一個組件的圖表樣式裡面只會設定一次，存在component chart的表中。那這表示說如果今天你可能在雙北的組件裡面，你可能調整了這個圖表的樣式。在所有的程式中，該組件只要是同一個index裡面都會同步調整。所以大家在開發上就是要建議要先構思說在不同程式間要如何同步的顯示的這個內容。然後以及要考慮到這個資料格式是不是有互相兼容。所以要避免說你可能在雙北的情境下，你可能要呈現圓表圖。
2. 但是在臺北市的部分，你可能沒辦法呈現圓表圖。因為資料格式不同。所以大家在組件的設計上，可能都要先想好在我如何應用雙北跟雙北的資料。因為雙北其實就是臺北市跟新北嘛。所以你要想像你要怎麼應用新北的資料跟雙北的資料。那共同的呈現在相同的圖表樣式裡面。然後去避免說大家在組件呈現上有問題。那以上就是多程式組件顯示的裸辭跟資料結構的介紹。



1. 新版地圖組件的設定教學。那在地圖設定的部分，在新版的架構中，地圖顯示的部分目前還是控制在 Query Chart 當中。那依照畫面上呈現到就是說，因為 Query Chart 的不同，它在不同程式裡面，它有對應的地圖的 ID 的呈現。就可以設定不同的圖層，依照不同的程式。所以，例如說左邊這個，大家應該看得出來，這個就是一個雙倍的這個組件的地圖。那右邊這個部分就是臺北市的組件地圖圖，呈現。那這個部分我們直接看畫面可能會比較有感覺。那首先我們先切換到，呃，物色交通的部分這個範例。然後在地圖交叉比對裡面，呃，我們直接點 U-Bike 使用情況。就是剛剛大家在簡報裡面看到的這個畫面。
2. 那目前因為我們是在雙倍的組件呈現。所以我們在地圖的部分這邊，一開始 Default 呈現的就是雙倍的地圖。那可以很新鮮看到，在原本的架構之下，原本可能只有臺北的 U-Bike 站點的內容。但是在雙倍的圖層的部分，呈現的圖層，我們就可以直接把呃，呃，視野拉到，呃，視野拉到新北，新北市的區域的內容去做呈現。那選單的部分依舊可以控制這個圖層的呈現。我們直接點臺北市的圖層。大家可以看到就是依照在，呃，圖表裡面設定的部分。我們可以依照不同的線式去切換今天要顯示哪些圖層。所以這個直接就導轉到在地圖的部分就會直接呈現臺北市的U-bike的資訊
3. 那這個部分設定其實在DB裡面也是一樣我們再來查一次就是組件的部分一個是U-bike使用情況我們看到它index是這個我們直接複製來我們看一下它在QueryChart裡面的設置在QueryChart裡面其實在Map的部分它就是有一個是Map Config ID那在Map Config ID的部分它對應到的是ComponentMap裡面的設定
4. 那大家可以看到就是依照後面城市Metro-Type-8跟Type-8這兩個城市下面的是臺北的嘛上面的是雙北的那上面的部分就是雙北的圖層它對應的變號ID是99那臺北的部分就是70這時候我們來看一下我們預先設定好的這個ComponentMap的圖表對然後這個部分就是有
5. 剛對應的是70跟99那其實我們一個是Metro-Type-8這個就是雙北的U-bike的站點的資料然後上面這個Realtime就是臺北的對應的資料那設定的部分就是依照MapComponentMap原本的設定的方式這邊我們就不再詳細的說明那以上的就是
6. 新版的地圖組建在多城市架構下的設定方式那前面其實統整了整體這一次新版組建更新的內容又完整說明說依照不同程式在設定上我們在QueryChart要如何設定那最後再提醒大家就是在
7. QueryChart的部分City這個欄位目前的開源專案我們是針對我們目前只有提供臺北臺北市跟雙北的設定那分別對應的建子就是臺北跟Metro-Type-8
8. 所以大家在組建設計上就是在QueryChart的設置的時候要記得要設定這個欄位那詳細的建置的部分我在後面的組建會教大家組合針對新版的組建開發用
9. 翻例導圖的方式為各位介紹那這個章節就到這裡那謝謝大家感謝觀看



1. 這次，這一個課程主要要跟各位介紹要如何用新版的架構來建立一個新的雙北的組件那在開始介紹之前要先跟各位介紹這個公開資料來源的建議那主要的原因是因為新版的架構是 based在臺北市政府資訊局跟新北市政府資訊中心共同合作的2025年的這個雙倍共榮的黑客松那在這個部分呢我們主要會介紹兩個資料平臺
2. 一個是臺北市資料大平臺然後一個是新北市政府的資料開放平臺這兩個資源那我們直接切換到那這個就是臺北市資料大平臺那在這個資料目錄的部分我們可以看到其實在臺北市政府這邊有提供很多開放資料那它包含它提供比如說像是CSV啊或者是有些是API性質的
3. 這邊有不同格式的資料源可以去做採選比如說有CSV文件或者是JSON檔案或者是有些圖資的資訊那大家可以在臺北市資料大平臺可以針對有些想取用的內容可以依照不同的主題去瀏覽大家可以在這個資料平臺裡面去找到有想要納管的這些資料去做臺北市去做程式儀錶板這個組件的優化
4. 那第二個部分是新北市政府的資料開放平臺那新北市政府也提供了很多多元的這個資料多元主題的資料包含比如說像這些主題採稅、質詢等等這些資料主題那也是有提供資料目錄的部分可以去做資料的查詢
5. 那這個部分就會針對就是在範例組件的部分就會針對這個公開資料就是這些我們範例組件的部分就會針對就直接去用這兩個資料平臺的資料去搭建一個雙倍的組件雙倍功榮的組件那這次在這次的課程
6. 其實會主要聚焦在我們剛剛前面提到的比如說架構的更新跟組件更新的說明跟介紹所以這一次的教大家如何建組件的部分會用一個範例導圖的方式去進行介紹那包含就是比如說再次
7. 像是在資料庫建資料或者說把圖表的數據匯入的部分其實可以參照過去的臺北城市儀表版開發指南第15、16章建立一個新的組件在當中有針對比如說資料寫入或者是
8. 組件比如說在component裡面去寫字進去就是要如何建立的部分會有一個更詳細的教學那在這部分我會針對信辦組件它在就是用範例導圖的方式我們直接看我們按照
9. 按照後面會介紹到性扳組件的建制步驟的流程他要先到哪裡去建資料那一步一步教各位用範例的方式去呈現那我們現在來看我們待會導讀的我們目前有開源的一個範例的資料他其實就是剛剛前面有看到的浮揚比以及老化指數的一個組件的內容那我們在資料的部分我們可以直接我們link到臺北市第一個是臺北市因為我們要選製作一個雙倍的組件所以我們要先看臺北市在臺北市的部分他有沒有浮揚比跟老化指數相對應的開源的資料
10. 那在臺北市的部分目前他就是提供CSV格式所以我們可以先預覽那他這邊有個資料存取的網址那我們就直接點擊下載那我們下載到這個資料夾那其實因為我前面其實已經有準備好就是我先給各位看一下這個內容就是一開始他從臺北市資料大平臺裡面下載下來他可能名詞的部分都是已經預先建置好的就是他比如說他有統計的期間
11. 區間然後他的一些比如說幼齡人口數然後一個人口比例然後我們主要要拿的其實就是老化指數跟復揚比我們要拿復揚比跟老化指數嘛所以我們OK那我們剛剛就是已經獲取臺北市的資料
12. 對這是臺北市的資料但是因為我們要建置到Database裡面我們其實需要這個名欄位的名稱我們需要替換所以我們就我其實有先準備了一個檔案然後這個檔案就是這個檔案就是這個
13. 所以我已經把上面的欄位名稱已經都替換了對然後我也加了加了一個叫做Database這個部分是用來我們來記錄說這個資料更新的這個時間所以這是很簡單的就是CSV那後續就是再匯到就是
14. 臺北城市儀標版的資料庫的部分我們剛剛有提到其實可以去參照一下在過去的開發建置指南裡面第15章的部分他其實有教大家怎麼把CSV如果有遇到中文編碼的問題其實可以直接參考就是他的怎麼轉編碼
15. 然後怎麼匯辱這邊我就不多做解釋但是我這邊會另外去提到另外一個就是說我們今天在另外一種資料取用方式是在新北市通修橋
16. 第二個部分就是我要介紹就是另外一種資料取用方式是 API就是我們直接用 API 調用因為過去之前的範例是提到是用 CSV 用匯入的那我這邊這一次的課程裡面我們可以我有另外準備了一個內容是我們要怎麼用 API 的形式去互調這個資料那這個部分我用新北市就是在這個扶養比跟老化指數的部分然後我們針對新北市政府的資訊去做demo
17. 我目前放的連結就是它的 API但是我們還是看一下新北市政府的資料開放平臺的這個限住人口年齡分佈那它的這個目錄資料目錄在這裡面我們可以拿到負養比跟老化指數那我們直接複製這個資料集名稱那我們直接到 Open就是它在下面的部分
18. 我們有提供不同的借接方式比如說它有 JSON或者說它這邊一樣有提供 CSV 的借接方式但我們這部分主要要介紹的是這個 API所以它這邊有 API 的部分我們直接導到這一點那新北市政府其實有提供這樣的 Open Data 的 API去做連結但是因為我們剛剛看到它其實資料來源是新北市政府的主基礎所以我們直接切換到主基礎的部分
19. 所以這邊就是主基礎所提供的 API我們這邊可以我們這邊可以我們全域搜尋一下那我們這邊第一次我們要限住人口年齡分佈來查就我們看到它有一個 API 是 CSV 的但是我們要找到的是JSON格式的 API那我們把它點開
20. 那在這個部分呢我們可以點一下 Try it out那裡面就會釋打這個 API 的內容那我們這邊直接execute 這個我們直接去執行這個 API大家可以看到它這邊 trigger 的request的 URL 的部分再放大一點request 的部分是
21. 的 API 內容是這樣然後下面的 response 就是它會傳的這個內容我們可以看到剛剛有看到剛剛做說明的就是我們有老化指數33 跟 31 就是這個復仰比的部分那我這邊在程式的部分我寫了一支程式然後這個程式的內容
22. 這個程式的內容它是一個簡單的 request 的的範例用 Python 寫的那在這個部分我們就是直接調用就是剛剛的 API 的部分那大家在程式因為這是我自己寫的程式內容所以大家如果有興趣的話
23. 可以直接就是直接就是按影片按個暫停然後可以參照一下我寫的這個邏輯跟內容有需要的話就歡迎大家去做取用那首先我們就是就是把一些套件 info 進來就是我們
24. 需要的 request 的套件然後跟我們現在要把它轉成data frame的內容然後還有一個就是時間的套件那我們就設定我們要打的這個 API因為剛剛打了這個 API就會回傳這個結果
25. 對但是因為我們其實它的資料比數可能會超過100 筆就是實際上所以我們就把時間設一個 5000然後這邊就設定我的輸出因為我要輸出成 CSB 檔其實跟臺北市政府一樣對
26. 跟臺北市臺北市政府資料大平臺剛剛那個資料員一樣我們都把傳成 CSB 好對那他當然也剛剛有提供直接提供 CSB 下載這部分我就是透過 API 的方式去把 JSON 格式的內容
27. 轉換成 CSB 的檔案對那我們這邊就直接用 request 去 get這個 API 的 URL 的資料那拿到資料這個 raw data就是我們拿到的資料的結果但是我
28. 剛剛在資料裡面給各位稍微看一下就是說在資料裡面它其實有不同維度在 FIELD 1 的部分有不同維度的統計資料所以我們這邊只有特別搜尋
29. 特別去篩選這個總計的部分所以在資料表裡面我們就是直接篩選總計然後把時間欄位去做一個處理去裝換把明國年裝換成西元年這樣然後欄位的部分我們跟剛剛
30. 臺北市政府的就是臺北市的那個佛陽比跟老化指數的資料一樣我們把這個欄位重新命名然後去移除掉我可能不需要的欄位然後把這些欄位然後把這些欄位去存到
31. 把這個 CSP去存到我們的目標資料夾然後我們執行那執行完之後我們就可以去這個資料夾的部分把這個欄位重新命名然後去移除掉我可能不需要的欄位然後最後把這些欄位去存到把這個 CSB 去存到我們的目標資料夾
32. 然後我們自行那自行完之後我們就可以去這個資料夾的部分資料夾的部分去看，就是這是剛剛建置的「New Taipei」的資料那其實這邊就會看到就是我們這邊已經有整理好的時間，然後跟各個指標的指數那這邊就會拿到我們的老化老化指數跟不陽比
33. 接下來進到就是「主建建置」的流程那「主建建置」的流程主要分為大致上可以分主要會分為五步啦就是第一個部分是我們要在「Compost」因為我們主要分為五步然後我們
34. 我們首先先到這個「Component」的部分那我們因為我們是做範例導作這邊我就不另外去做資料協錄的部分那我們先看到就是因為其實在範例組件這個其實已經建好它就是「復仰筆記」然後「指數」
35. 那實際上「主建建置」的流程第一個步驟其實你應該先來建這個「Component」那我們要去設定它的「Index」就是這個「Index」在後面的就是對應的表有對應到的比如說「Component」「Component」裡面都需要設定這個「Index」的部分都是要共同的名稱
36. 這樣才不會有問題所以我們先建立一個就是在這個部分我們就假設我們建立一個叫做「復仰筆記老化指數」然後我們設定一個「Index」叫「Dependency 筆線 Agent」那設定好之後第二個步驟我們就是要到「Query Chart」裡面我們到「Query Chart」裡面去看那其實在這個部分我們就是依照不同程式我們需要建兩筆資料
37. 一個是一個是「臺北」一個是「Metro 臺北」那我們就依照不同程式的設置我們可以設定這「Index」是固定也就是跟對應你的「Component」的設定那後面就是依照你比如說你有歷史資料的「Config」或者是說你有
38. 對應的圖層假設你有地圖的話如果你有地圖的話這邊就需要設定對應的圖層這前面也有其實都有稍微介紹過那這個部分就是分別設定你的時間它是它資料收入是從什麼時候到什麼時候那
39. Update Frequency 的部分就是資料更新頻率資料更新的頻率的部分都可以去做設計然後再來就是不同的來源比如說它那裡收學可能會不一樣可能會不一樣然後再來是它的秒數就是對應的短的秒數跟長的秒數
40. 所以就是臺北跟雙北都要設定一個好那Use case 的話兩個就是臺北跟雙北的使用情形也要不一樣就是依照對應程式需要設置的內容去調整然後再來是LINKLINKS就是它的資料來源那這個部分就會因為我們其實在系統上已經有設計是說臺北資料大平臺
41. 它在上面會顯示在臺北資料大平臺的話就是Data大臺配這個部分會顯示那如果是新美式政府公開資料平臺的話這邊會顯示Data.NTP11這個是系統目前建置的但是如果有使用這兩個資料平臺以外的就是比如說一些開源的資料平臺它這邊就會顯示成其他
42. 這個不就不另外說明瞭對那在第二部分就是Query trial的部分設置到這邊基本上快完成然後後面就是設定對應的Query的形式跟它Query的的語法CQL語法是什麼然後最後就是
43. 它的CT的部分就是一定要設定臺北或者是MATRO臺北所以這是Btian的一個欄位然後我這邊補充一下就是說如果你要設雙北組件的話你一定要有一個對應的臺北組件對
44. 所以因為它在實際上它在拉取的時候它會去顯示說它會去檢索這個組件它有沒有臺北的資料去做呈現如果說你這邊沒有先預先設一個臺北的就是你如果這個
45. Query trial裡面你有MATRO臺北但它沒有臺北的資料的話它這邊在組件顯示可能會有問題所以如果今天在雙北組件在建置的時候一定要記得如果你要建MATRO臺北的資料預先一定要有一個臺北的設定
46. 它在組件呈現上你切換到臺北市的時候才不會有問題然後接下來就是會到component char在 component char在 component char的部分就是你在 component char的部分
47. 就是你在 component char的部分就是你OK在 component char的部分就是如果突然剛剛所介紹的就是說一個就不管你query char裡面
48. 有幾個城市的設定那目前只有開放臺北跟雙北那不管你有有臺北也好或是說你臺北跟雙北都有你在 component char的設定裡面
49. 一定只會有一筆資料因為它就會對應到的是你的 component的組件的 index對於 index的部分在 component char裡面它這邊就是有一個dependency agent那這個 color的部分
50. 就是設定你這個圖表的顏色的顏色的序列這樣然後 type的部分就會設定你今天要呈現什麼圖形這個部分可以參考我們的技術文件
51. 就是內容其實都有更詳細的描述有哪些圖表類型都可以選我這邊因為是針對架構的更新的部分說明所以我這邊就不再
52. 細數了對所以基本上如果在沒有地圖組件的情況之下在沒有地圖的資訊呈現的情況之下我們就只會有只需要設置 component跟 query char
53. 然後還有 component char基本上在雙倍共容在意表應該說在新版的儀錶板架構裡面雙倍組件的呈現就不會有問題那我們在登錄之後
54. 我們進到這個後臺的部分我們在臺北儀錶板裡面那針對臺北的儀錶板裡面如果已經要新增組件到這個臺北的儀錶板公開儀錶板
55. 公開儀錶板我在這個介面在這個瀏覽機我們只會看到有臺北在 query char裡面有City等於臺北的這些
56. 組件才會在這邊顯示這意味著就是臺北的儀錶板只會看到臺北的組件那如果說今天是在雙倍的儀錶板裡面裡面我今天在這個的清單
57. 可以加這個組件的清單裡面只會呈現由Metro Type-Hade這個組件設計所以在這個部分比如說我在這個部分
58. 我們可以針對就剛剛看到的這個浮揚比跟老化指數如果剛這三張表建立好之後這邊就其實就看得到這個內容對那
59. 如此就是就是有就是範例導圖的方式我們就是會再再衝突再
60. 簡單再講一遍就是說我們在第一個步驟就是到Component裡面去建表去建這個組件的內容那第二個部分就是到Query Char裡面去設定臺北跟雙倍的
61. 對應的要呈現的這個內容那用Intel X部分要跟Component一模一樣然後最後我們再到Component Char裡面去設置對應的要顯示的
62. 這個圖表的類型跟它的跟它的顏色可以就是顏色序列等等然後以及這個圖表要呈現的單位那以上就是這個組件建置新版組件的一個
63. 教學的內容那下一個章下一個章節就是我會跟各位介紹這個新的圖表的叫文字單位圖的介紹那謝謝大家謝謝大家



1. 這個章節要為各位介紹新的圖表類型文字單位圖這是新版本的圖表樣式就是新的圖表樣式那就是文字單位圖那文字單位圖大家可以在畫面上面看到的它其實就是有一個文字描述然後跟對應的這個文字比如說它可能是一個指標的類型然後它有對應的數值
2. 以及對應的單位那根據就是在Charl Config裡面它其實可以設定三種顏色它就是在ComponentChar裡面的Charl Config它可以設定三種顏色分別對應文字跟它的數值跟它的單位那在依照就是圖表的這個資料維度的部分
3. 它它是它的文字單位圖它是採用這個三維的資料然後它在那個語法的所以ComponentCharl
4. 所以ComponentCharl調用的語法上它需要有一個欄位叫做iCon就是用iCon這個欄位來設計說依照不同的指標它可能有
5. 對應的單位需要去做呈現那就是讓整體這個文字單位圖的就像是一個指標的集合它可以在同一個圖表裡面呈現不同指標不同指標的單位跟內容這樣然後可以更清晰的在這個圖表裡面去為這個數據去
6. 去提供更完整的上下文那我們直接看DB好了那我們直接看DB的這邊從頭再來我們直接看DB的的表的內容
7. 那我們剛剛看到的長照指標其實對應到的就是agentagent KPI這個index的的內容那我們直接看QuerityCharlQuerityCharl裡面其實可以看到它其實在設定上
8. 我們在資料設定上我們的QuerityType必須設為剛剛提到的三位資料它這邊設定就是3D那在資料調用的方式我直接給各位看這個
9. SEQL取用的語法那我們直接檢索那依照設定的部分我們三位的資料需要設定的是一個是Y軸一個就是對應你的指標指標的內容然後
10. ICON的部分這邊就可以依照你前面設定的這個指標它的單位是什麼就可以設定不同的單位你有可能是人數你有可能是你有可能是PAR像是這個是PAR的
11. 就是比例嘛上升的多少PAR下降的多少PAR有可能今天可能是多少圓或者是說就是依照你前面指標的部分就可以設定對應的
12. ICON的部分那在DATA部分就是對應的數值所以我們回到畫面上面在前面的部分就是這就是新版的這個文字單位圖那設定的話
13. 就是依照你的前面的指標然後對應的單位跟對應的數值跟對應的單位那在那在這個因為我們其實在Component Char裡面就有它這個
14. 格式我們Component Char裡面這邊就是設定就是說你的Index的內容它是就Agent KPIAgent KPI這個然後你的Type的部分
15. 它在單位上它就都有Text Unit Char那單位的部分因為原先在原本呢其他的圖表類型設置的部分它的Unit都是
16. 對應你的圖表的單位但是我們把這個單位的部分移到剛剛看到的這個Query Char裡面就是直接在資料取用的部分
17. 去做設置那一方面是為了要讓這個文字單位圖的部分可以移照不同的文字內容可以呈現不同的單位所以大致上設置
18. 就是文字單位圖的設置大致上就是就是就是這樣看一下對大致上就會是這樣的一個內容然後
19. 我們看一下文件好了那文件裡面有針對前端的部分新的圖表樣式那這文字單位圖的更新我相信也會在文件上面去做說明在圖表類型的部分一方面就是在這個
20. 文字單位圖的部分會做一個說明就是說我們依照對應的configchartconfig我去設定就是它最多可以設在那種顏色就是針對文字數值跟單位
21. 然後它是一個三維的數據結構那在剛剛看到從DB裡面看到它其實要設計的設定的其實就是它這個 name 的部分對應的其實就是
22. Y軸然後 data對應的數值然後 icon 的部分就是對應的是單位對所以大家在鍵字上就可以參照剛剛的
23. DB架構去設置這個文字單位圖那就可以得到依照的需求可以設定對應的呈現的內容那以上的部分就是文字單位圖的
24. 介紹然後最後總結一下因為這是本次的課程裡面最後一個章節的內容那以上就是本次臺北城市儀表版3.0關於
25. 這個架構的更新更新組件的設定的一個介紹那希望大家可以透過這份教學可以更快速的理解就是這一次架構異動的部分那也歡迎大家
26. 如果有新的想法也可以來信來跟我們說說你對於這個開原專案的一些新的idea那最後謝謝大家花時間可以聆聽到這邊
27. 謝謝大家收看


